services:
  postgres_panel:
    image: postgres:16-alpine
    container_name: forte_postgres_panel
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${PANEL_POSTGRES_DB:-forte_panel}
      POSTGRES_USER: ${PANEL_POSTGRES_USER:-forte_panel}
      POSTGRES_PASSWORD: ${PANEL_POSTGRES_PASSWORD:?PANEL_POSTGRES_PASSWORD is required}
    volumes:
      - forte_postgres_panel_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${PANEL_POSTGRES_USER:-forte_panel} -d $${PANEL_POSTGRES_DB:-forte_panel}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - panel-network

  redis_panel:
    image: redis:7-alpine
    container_name: forte_redis_panel
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - forte_redis_panel_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - panel-network

  forte-panel:
    image: ghcr.io/geordptoroy/forte-panel:latest
    pull_policy: always
    container_name: forte_panel
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DEMO_MODE: ${DEMO_MODE:-false}
      FORTE_API_KEY: ${FORTE_API_KEY:?FORTE_API_KEY is required}
      WEBHOOK_SIGNING_SECRET: ${WEBHOOK_SIGNING_SECRET:?WEBHOOK_SIGNING_SECRET is required}
      DATABASE_URL: postgresql://${PANEL_POSTGRES_USER:-forte_panel}:${PANEL_POSTGRES_PASSWORD}@postgres_panel:5432/${PANEL_POSTGRES_DB:-forte_panel}
      REDIS_URL: redis://redis_panel:6379/0
      RUN_MIGRATIONS: "true"
      PAPI_BASE_URL: ${PAPI_BASE_URL:-http://pastorini_api:3000}
      PAPI_API_KEY: ${PAPI_API_KEY:?PAPI_API_KEY is required}
      PAPI_SEND_MESSAGE_PATH: ${PAPI_SEND_MESSAGE_PATH:-/messages}
      N8N_BASE_URL: ${N8N_BASE_URL:-http://n8n:5678}
      N8N_EVENTS_WEBHOOK_URL: ${N8N_EVENTS_WEBHOOK_URL:-}
      N8N_WEBHOOK_SECRET: ${N8N_WEBHOOK_SECRET:-}
      N8N_WEBHOOK_TIMEOUT_MS: ${N8N_WEBHOOK_TIMEOUT_MS:-10000}
      META_GRAPH_API_VERSION: ${META_GRAPH_API_VERSION:-v23.0}
      META_WHATSAPP_ACCESS_TOKEN: ${META_WHATSAPP_ACCESS_TOKEN:-}
      META_WHATSAPP_PHONE_NUMBER_ID: ${META_WHATSAPP_PHONE_NUMBER_ID:-}
    ports:
      - "${PANEL_PORT:-3002}:3000"
    depends_on:
      postgres_panel:
        condition: service_healthy
      redis_panel:
        condition: service_healthy
    networks:
      - panel-network
      - pastorini-network

  forte-panel-worker:
    image: ghcr.io/geordptoroy/forte-panel:latest
    pull_policy: always
    container_name: forte_panel_worker
    restart: unless-stopped
    command: ["./docker-entrypoint.sh", "worker"]
    environment:
      NODE_ENV: production
      DEMO_MODE: ${DEMO_MODE:-false}
      FORTE_API_KEY: ${FORTE_API_KEY:?FORTE_API_KEY is required}
      WEBHOOK_SIGNING_SECRET: ${WEBHOOK_SIGNING_SECRET:?WEBHOOK_SIGNING_SECRET is required}
      DATABASE_URL: postgresql://${PANEL_POSTGRES_USER:-forte_panel}:${PANEL_POSTGRES_PASSWORD}@postgres_panel:5432/${PANEL_POSTGRES_DB:-forte_panel}
      PAPI_BASE_URL: ${PAPI_BASE_URL:-http://pastorini_api:3000}
      PAPI_API_KEY: ${PAPI_API_KEY:?PAPI_API_KEY is required}
      PAPI_SEND_MESSAGE_PATH: ${PAPI_SEND_MESSAGE_PATH:-/messages}
      N8N_BASE_URL: ${N8N_BASE_URL:-http://n8n:5678}
      N8N_EVENTS_WEBHOOK_URL: ${N8N_EVENTS_WEBHOOK_URL:-}
      N8N_WEBHOOK_SECRET: ${N8N_WEBHOOK_SECRET:-}
      N8N_WEBHOOK_TIMEOUT_MS: ${N8N_WEBHOOK_TIMEOUT_MS:-10000}
      META_GRAPH_API_VERSION: ${META_GRAPH_API_VERSION:-v23.0}
      META_WHATSAPP_ACCESS_TOKEN: ${META_WHATSAPP_ACCESS_TOKEN:-}
      META_WHATSAPP_PHONE_NUMBER_ID: ${META_WHATSAPP_PHONE_NUMBER_ID:-}
      WORKER_INTERVAL_MS: ${WORKER_INTERVAL_MS:-1500}
      WORKER_BATCH_SIZE: ${WORKER_BATCH_SIZE:-10}
      WORKER_MAX_ATTEMPTS: ${WORKER_MAX_ATTEMPTS:-3}
      EVENT_WORKER_BATCH_SIZE: ${EVENT_WORKER_BATCH_SIZE:-10}
      EVENT_WORKER_MAX_ATTEMPTS: ${EVENT_WORKER_MAX_ATTEMPTS:-5}
    depends_on:
      postgres_panel:
        condition: service_healthy
    networks:
      - panel-network
      - pastorini-network

networks:
  panel-network:
    name: forte-panel-network
  pastorini-network:
    name: ${COMPOSE_PROJECT_NAME:-forte-local}_pastorini-network

volumes:
  forte_postgres_panel_data:
  forte_redis_panel_data:
